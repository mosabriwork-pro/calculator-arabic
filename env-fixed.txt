# THIS FILE HAS BEEN SANITIZED - DO NOT COMMIT SECRETS
# This file previously contained sensitive credentials and has been cleaned.
# To run the app locally, create a local env file named `.env.local` and
# add your real secrets there. Do NOT commit `.env.local` to git.

# Example placeholders (replace with your real values in .env.local):
# EMAIL_USER=you@example.com
# EMAIL_PASS=your-app-password
# APP_SECRET=your-app-secret
# ADMIN_PASSWORD=choose-a-strong-password
# NODE_ENV=development
# PORT=3002
# NEXT_PUBLIC_DOMAIN=localhost:3002

# Notes:
# - Keep secrets in `.env.local` (listed in .gitignore).
# - Remove this file or keep it as a reminder; it contains NO real secrets now.
